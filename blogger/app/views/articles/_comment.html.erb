<div>
  <h4> ~ <%= comment.author_name %></h4>
  <p class="comment"><%= comment.body %> </br> </br> <i>
    <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later </i> </p>
</div>
